// import { IMaskInput } from "react-imask";

// const MaskedInput = ({ field, form, ...props }) => {
//   return <IMaskInput {...field} {...props} onAccept={(value) => form.setFieldValue(field.number, value)} />;
// };

// export default MaskedInput;
